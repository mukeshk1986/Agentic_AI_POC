CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.ref_risk_model (
    MODEL_ID SMALLINT NOT NULL,
    MODEL_DESCRIPTION VARCHAR(100),
    PROGRAM_MODEL VARCHAR(10),
    PROGRAM VARCHAR(3),
    CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA;

CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.ref_risk_variable (
    VARIABLE_ID INT NOT NULL,
    MODEL_ID SMALLINT NOT NULL,
    SAS_MODEL_VERSION_ID SMALLINT,
    VARIABLE_CODE VARCHAR(50),
    DESCRIPTION VARCHAR(180),
    MIN_AGE_LAST SMALLINT,
    MAX_AGE_LAST SMALLINT,
    GENDER VARCHAR(1),
    HCC_FLAG VARCHAR(1),
    INT_FLAG VARCHAR(1),
    DEMO_FLAG VARCHAR(1),
    AGEGENDER_FLAG VARCHAR(1),
    INCLUDE_IN_COUNTS VARCHAR(1),
    CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA;

CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.ref_risk_variable_detail (
    VARIABLE_ID INT NOT NULL,
    MODEL_ID SMALLINT NOT NULL,
    HCC_ID INT,
    SAS_MODEL_VERSION_ID SMALLINT,
    TYPEID TINYINT,
    CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA;

CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.ref_method_metadata (
    METHOD_ID SMALLINT NOT NULL,
    PROGRAM VARCHAR(3),
    PROGRAM_MODEL VARCHAR(10),
    CLAIM_TYPE VARCHAR(1),
    ICD_VER VARCHAR(2),
    CLAIM_CD_TYPE VARCHAR(25),
    CLAIM_CD VARCHAR(20),
    CLAIM_CD_MODIFIER_TYPE VARCHAR(25),
    CLAIM_CD_MODIFIER VARCHAR(20),
    CC_ID INT,
    SUBCC BIGINT,
    GENDER VARCHAR(1),
    EXCLUSION_TYPE_1 VARCHAR(25),
    EXCLUSION_1 VARCHAR(20),
    EXCLUSION_TYPE_2 VARCHAR(25),
    EXCLUSION_2 VARCHAR(20),
    AGE_FIRST_START SMALLINT,
    AGE_FIRST_END SMALLINT,
    AGE_LAST_START SMALLINT,
    AGE_LAST_END SMALLINT,
    PROGRAM_CLAIM_TYPE SMALLINT,
    QUALIFIED_CLAIM SMALLINT,
    BUNDLED_CHECK SMALLINT,
    EFFECTIVE_DATE DATE,
    EXPIRATION_DATE DATE,
    PERCENT_WEIGHT DECIMAL(7,6),
    CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA
PARTITIONED BY (METHOD_ID, PROGRAM);

CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.ref_mor_field_position (
    PROGRAM STRING,
    PROGRAM_MODEL STRING,
    FIELD_DESCRIPTION STRING,
    RECORD_TYPE STRING,
    MODEL_VERSION STRING,
    FIELD_POSITION INT,
    MOR_RECONCILIATION_FLAG TINYINT,
    VARIABLE_ID INT,
    VARIABLE_CODE STRING,
    HEALTH_EVENT_CC_ID INT,
    HEALTH_EVENT_MODEL_VERSION STRING,
    CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA;

CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.ref_mor_health_event_confidence (
    CHRONIC BOOLEAN,
    ACUTE BOOLEAN,
    CURRENT_YEAR BOOLEAN,
    PRIOR_YEAR BOOLEAN,
    SECOND_YEAR_PRIOR BOOLEAN,
    CONFIDENCE DECIMAL(3,2),
    CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA;

CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.version_weightage_risk_year (
    MODEL STRING,
    TIME_PERIOD BIGINT,
    VERSION STRING,
    WEIGHTAGE DOUBLE,
    CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA;

CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.adjustment_factors (
    VERSION STRING,
    NORMALIZATION_FACTOR DOUBLE,
    CODING_PATTERN_ADJUSTMENT DOUBLE,
    MODEL STRING,
    TIME_PERIOD INT,
    CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA;

CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.coefficient_scores (
    VERSION STRING,
    MODEL STRING,
    NAME STRING,
    DESCRIPTION STRING,
    SCORE DOUBLE,
    CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA;

CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.community_model_rules (
    MODEL STRING,
    VERSION STRING,
    CONDITION STRING,
    SEGMENT_MODEL STRING,
    INCL_SUPPLEMENTAL_MMR STRING,
    CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA;

CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.hierarchy_config (
    VERSION STRING,
    PARENT INT,
    CHILD STRING,
    MODEL STRING,
    CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA;

CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.icd_hcc_mapping (
    DIAGNOSISCODE STRING,
    DESCRIPTION STRING,
    `CMS-HCC-ESRD-MODEL-CATEGORY-V21` BIGINT,
    `CMS-HCC-ESRD-MODEL-CATEGORY-V24` BIGINT,
    `CMS-HCC-MODEL-CATEGORY-V22` BIGINT,
    `CMS-HCC-MODEL-CATEGORY-V24` BIGINT,
    `CMS-HCC-MODEL-CATEGORY-V28` BIGINT,
    `RXHCC-MODEL-CATEGORY-V05` BIGINT,
    `RXHCC-MODEL-CATEGORY-V08` BIGINT,
    `CMS-HCC-ESRD-MODEL-CATEGORY-V21-FOR-2025-PAYMENT-YEAR` STRING,
    `CMS-HCC-ESRD-MODEL-CATEGORY-V24-FOR-2025-PAYMENT-YEAR` STRING,
    `CMS-HCC-MODEL-CATEGORY-V22-FOR-2025-PAYMENT-YEAR` STRING,
    `CMS-HCC-MODEL-CATEGORY-V24-FOR-2025-PAYMENT-YEAR` STRING,
    `CMS-HCC-MODEL-CATEGORY-V28-FOR-2025-PAYMENT-YEAR` STRING,
    `RXHCC-MODEL-CATEGORY-V05-FOR-2025-PAYMENT-YEAR` STRING,
    `RXHCC-MODEL-CATEGORY-V08-FOR-2025-PAYMENT-YEAR` STRING,
    CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA;

CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.interaction_coefficients (
    MODEL STRING,
    VERSION STRING,
    INTERACTION_GROUP_PRIMARY STRING,
    INTERACTION_GROUP_SECONDARY STRING,
    SCORE DOUBLE,
    CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA;

CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.procedure_codes (
    HCPCS_CPT_CODE STRING,
    DESCRIPTION STRING,
    CY2025 STRING,
    CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA;


----DDL for ${catalog}.${schema_reference}.time_period
CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.time_periods (
    TIME_PERIOD BIGINT,
    BEGIN_DATE DATE,
    END_DATE DATE,
    TIME_PERIOD_TYPE_CD STRING,
    CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA;
-- DDL for ${catalog}.${schema_reference}.ref_risk_proc_qualifying --
CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.ref_risk_proc_qualifying (
  CPT_AND_HCPCS_CD STRING,
  EFFECTIVE_DATE DATE,
  EXPIRATION_DATE DATE,
  CMS_ELIGIBILITY_FLAG STRING,
  CREATED_DATE           TIMESTAMP,
  CREATED_BY             STRING,
  UPDATED_DATE           TIMESTAMP,
  UPDATED_BY             STRING)
USING DELTA;

CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.ref_risk_hcc (
    CC_ID INT,
    PROGRAM VARCHAR(3),
    CC_CODE VARCHAR(50),
    CC_DESCRIPTION VARCHAR(200),
    CATEGORY_ID SMALLINT,
    Notes VARCHAR(100),
    CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA;

CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.ref_risk_subcc (
    SUBCC BIGINT,
    SUBCC_Description VARCHAR(200),
    CATEGORY_ID SMALLINT,
    OUTLOOKSTART INT,
    OUTLOOKEND INT,
    CHRONIC TINYINT,
    CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA;

CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.ref_sas_model (
    SAS_MODEL_VERSION_ID SMALLINT,
    SAS_MODEL_VERSION VARCHAR(20),
    SAS_MODEL_YEAR SMALLINT,
    PROGRAM VARCHAR(3),
    PROGRAM_MODEL VARCHAR(10),
    VERSION_CODE VARCHAR(10),
    CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA;

CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.ref_visit_type_rbcs (
    VISIT_TYPE_ID SMALLINT,
	VISIT_TYPE_DESC VARCHAR(100),
	RBCS_CD VARCHAR(8),
	RBCS_FAMILY_DESC VARCHAR(100),
	HCPCS_CD VARCHAR(6),
	HCPCS_CD_ADD_DT DATE,
	HCPCS_CD_END_DT DATE,
	CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA;

CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.ref_place_of_service_tob (
    TYPE_OF_BILL_CD VARCHAR(3),
	PLACE_OF_SERVICE_CD VARCHAR(3),
	POS_DESC VARCHAR(100),
	CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA;

CREATE TABLE IF NOT EXISTS ${catalog}.${schema_reference}.ref_bid_mapping (
    PREFIX VARCHAR(2),
    STARTING_SERIES VARCHAR(16),
    CLAIM_TYPE VARCHAR(10),
    DATA_TOPIC VARCHAR(100),
    CREATED_DATE TIMESTAMP,
    CREATED_BY STRING,
    UPDATED_DATE TIMESTAMP,
    UPDATED_BY STRING
)
USING DELTA;